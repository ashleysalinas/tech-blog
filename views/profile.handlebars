<section>
<h2>Hi, {{name}}!</h2>
<h2>Your Posts</h2>
{{#each posts as |post|}}
<div>
    <p>{{post.post_title}}</p>
    <p>Date: {{format_date post.date_posted}}</p>
    <p>{{post.post_text}}</p>
    <button>Edit</button>
    <button>Delete</button>
</div>
{{/each}}
